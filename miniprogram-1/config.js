const config = {
  // API接口基础路径 - 生产环境
  baseUrl: 'https://www.tzy.lol',  // 使用你的域名
  
  // API路由
  api: {
    login: '/api/login',
    groups: '/api/groups',
    members: '/api/members',
    score: '/api/score'
  }
}

export default config 