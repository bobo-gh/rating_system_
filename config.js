const config = {
  // API接口基础路径 - 生产环境
  baseUrl: 'https://www.aibobo.tech/api',  // 替换为你的域名
  
  // API路由
  api: {
    login: '/api/login',
    groups: '/api/groups',
    members: '/api/members',
    score: '/api/score'
  }
}

export default config 